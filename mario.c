//Library catalog
#include <stdio.h>
#include <cs50.h>

int main(void)
{
   do{

    int n = get_int("Height: ");
    }
    while (n < 1);
    {
        n = get_int("Height: ");
    }
    printf("#, n + 1);
}
